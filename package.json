{
  "dependencies": {
    "ejs": "^6.0.1",
    "express": "^5.2.1"
  },
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Tidak perlu build untuk backend Node.js'", 
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}